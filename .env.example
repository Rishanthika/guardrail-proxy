# Copy to .env and adjust as needed. All of these map 1:1 to fields in
# config/settings.py and are optional — sensible defaults are baked in.

# environment=production
# database_url=postgresql+asyncpg://user:pass@host:5432/guardrail
# dry_run_override=true
# log_level=DEBUG
# policy_path=/app/config/policy.yaml
